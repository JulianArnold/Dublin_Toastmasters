<%= form_for(@how_to_find_u) do |f| %>
  <% if @how_to_find_u.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@how_to_find_u.errors.count, "error") %> prohibited this how_to_find_u from being saved:</h2>

      <ul>
      <% @how_to_find_u.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :image_url %><br />
    <%= f.text_field :image_url %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
